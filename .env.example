# ============================================================================
# Design Token Manager - Environment Configuration
# ============================================================================
# Copy this file to .env and fill in your actual values
# IMPORTANT: Never commit the .env file to version control

# ============================================================================
# SUPABASE CONFIGURATION (Optional)
# ============================================================================
# Enables multi-project collaboration and sandbox editing features.
# If not configured, the app runs in local-only mode (Git-based tokens only).
#
# To set up Supabase:
# 1. Create a project at https://supabase.com (free tier available)
# 2. Run the schema from supabase-schema.sql in SQL Editor
# 3. Get your credentials from Settings → API
# 4. Restart your dev server after adding credentials
#
# Test connection: npm run test:supabase
# ============================================================================

# Your Supabase Project URL
# Format: https://[project-id].supabase.co (no trailing slash)
# Find in: Supabase Dashboard → Settings → API → Project URL
VITE_SUPABASE_URL=https://your-project.supabase.co

# Your Supabase Anonymous/Public Key
# Format: Long JWT token starting with eyJ...
# Find in: Supabase Dashboard → Settings → API → Project API keys → anon public
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ============================================================================
# MULTI-PROJECT MODE (PRD 0051)
# ============================================================================
# Required for database migrations and CI/CD automation
# ⚠️ SECURITY WARNING: This key bypasses RLS policies. Keep it secret!
# Find in: Supabase Dashboard → Settings → API → service_role key (secret)
SUPABASE_SERVICE_KEY=your-service-role-key-here

# ============================================================================
# DEVELOPMENT AUTHENTICATION BYPASS (PRD 0058)
# ============================================================================
# ⚠️ DEV ONLY: Bypass authentication for faster local development
# Automatically logs you in as a mock admin user without Supabase login
# IMPORTANT: This will NOT work in production builds (NODE_ENV check)
#
# To enable: Set VITE_DEV_AUTH_BYPASS=true
# To disable: Set to false or remove the variable
# ============================================================================

# Enable/Disable dev auth bypass
VITE_DEV_AUTH_BYPASS=false

# Mock user email for development (used as user identity)
DEV_AUTH_USER_EMAIL=dev@example.com

# Mock user display name
DEV_AUTH_USER_NAME=Dev Admin

# ============================================================================
# ADVANCED CONFIGURATION (Optional)
# ============================================================================
# These are typically not needed but can be customized if required

# Project ID (auto-detected from Supabase, override if needed)
# PROJECT_ID=

# Custom API endpoint (if using a proxy or custom backend)
# API_ENDPOINT=http://localhost:3001
